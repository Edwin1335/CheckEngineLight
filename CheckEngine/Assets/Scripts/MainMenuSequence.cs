using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainMenuSequence : MonoBehaviour
{ 
    /* Start is called before the first frame update
    void Start()
    {
        StartCoroutine(LoadVideos());
    }

    // Update is called once per frame
    IEnumerator LoadVideos()
    {
        Handheld.PlayFullScreenMovie("MainMenu_background_video", Color.black, FullScreenMovieControlMode.CancelOnInput);
        yield return new WaitForEndOfFrame();
        yield return new WaitForEndOfFrame();

        //second vid
        Handheld.PlayFullScreenMovie("Intro", Color.black, FullScreenMovieControlMode.CancelOnInput);
        yield return new WaitForEndOfFrame();
        yield return new WaitForEndOfFrame();
    }   */
}
